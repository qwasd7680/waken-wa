generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

model AdminUser {
  id           Int      @id @default(autoincrement())
  username     String   @unique @db.VarChar(100)
  passwordHash String   @map("password_hash")
  createdAt    DateTime @default(now()) @map("created_at")

  @@map("admin_users")
}

model ApiToken {
  id         Int       @id @default(autoincrement())
  name       String    @db.VarChar(200)
  token      String    @unique @db.VarChar(64)
  isActive   Boolean   @default(true) @map("is_active")
  createdAt  DateTime  @default(now()) @map("created_at")
  lastUsedAt DateTime? @map("last_used_at")

  @@map("api_tokens")
}

model ActivityLog {
  id           Int       @id @default(autoincrement())
  device       String    @db.VarChar(200)
  processName  String    @map("process_name") @db.VarChar(200)
  processTitle String?   @map("process_title")
  startedAt    DateTime  @default(now()) @map("started_at")
  endedAt      DateTime? @map("ended_at")
  metadata     Json?
  createdAt    DateTime  @default(now()) @map("created_at")

  @@map("activity_logs")
}

model SiteConfig {
  id            Int      @id @default(1)
  userName      String   @map("user_name") @db.VarChar(120)
  userBio       String   @map("user_bio")
  avatarUrl     String   @map("avatar_url")
  userNote      String   @map("user_note")
  historyWindowMinutes Int @map("history_window_minutes") @default(120)
  historyWindowHintText String @map("history_window_hint_text") @default("历史窗口：最近 2 小时（可在设置中调整）")
  appMessageRules Json?  @map("app_message_rules")
  pageLockEnabled Boolean @map("page_lock_enabled") @default(false)
  pageLockPasswordHash String? @map("page_lock_password_hash")
  currentlyText String   @map("currently_text") @db.VarChar(60) @default("currently")
  earlierText   String   @map("earlier_text") @db.VarChar(60) @default("earlier")
  updatesText   String   @map("updates_text") @db.VarChar(120) @default("updates every 30 seconds")
  adminText     String   @map("admin_text") @db.VarChar(30) @default("admin")
  createdAt     DateTime @default(now()) @map("created_at")
  updatedAt     DateTime @updatedAt @map("updated_at")

  @@map("site_config")
}
